default:
    extensions:
        Behat\Symfony2Extension\Extension:
            mink_driver: true
            kernel:
                env: test
                debug: true
        Behat\MinkExtension\Extension:
            default_session: 'symfony2'
            show_cmd: 'firefox %s'
    paths:
        features:  features
