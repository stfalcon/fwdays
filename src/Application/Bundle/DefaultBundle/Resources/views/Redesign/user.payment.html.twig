<div class="user-payment">
{% if ticket.payment.tickets.count > 1 %}
    <button class="icon-circle-close user-payment__remove"></button>
{% endif %}
    <div class="user-payment__left">
        <div class="user-payment__name">{{ ticket.user.surname }} {{ ticket.user.name }}</div>
        <div class="user-payment__email">{{ ticket.user.email }}</div>
    </div>
    <div class="user-payment__right">
    {% if ticket.hasDiscount %}
        <div class="user-payment__price">
            <span class="user-payment__price-strike">{{ 'payment.price'|trans({'%summ%':ticket.amountWithoutDiscount}) }}</span>{{ 'payment.price'|trans({'%summ%':ticket.amount}) }}
        </div>
        <div class="user-payment__discount">
        {% if ticket.hasPromoCode %}
            {{ 'payment.discount.cupon'|trans({'%summ%':ticket.getPromoCode.discountAmount}) }}&nbsp;
        {% else %}
            {{ 'payment.discount.member'|trans({'%discountAmount%':discountAmount}) }}&nbsp;
        {% endif %}
        </div>
    {% else %}
        <div class="user-payment__price">{{ 'payment.price'|trans({'%summ%':ticket.amount}) }}</div>
    {% endif %}
    </div>
</div>
