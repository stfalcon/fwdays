{% extends '@StfalconEvent/metatags.html.twig' %}

{% block title %}{{ one_news.title }} / {{ parent() }}{% endblock %}

{% block content %}
    <article>
        <h2>{{ one_news.title }}</h2>
        
        <div class="article-date-rubrics">{{ one_news.createdat|date('d M Y') }}</div>

        {{ one_news.text|raw }}
    </article>
        
    <div class="to-newslist-link"><a href="{{ path('news') }}">← <u>{{ 'news.to_the_list'|trans }}</u></a></div>
{% endblock %}