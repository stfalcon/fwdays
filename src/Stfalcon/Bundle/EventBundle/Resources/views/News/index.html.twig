{% extends '::event-no-news.html.twig' %}

{% block content %}
    <h2>Новости и анонсы</h2>

    <div class="news-list">
        {% for one_news in news %}
        <article class="intro">
            <h2><a href="{{ path('event_news_show', { 'news_slug': one_news.slug, 'event_slug': event.slug }) }}">{{ one_news.title }}</a></h2>
            <div class="article-date-rubrics">{{ one_news.createdat|date('d M Y') }}, <a href="{{ path('event_show', { 'slug': event.slug } )}}">Zend Framework Day</a></div>
            <p>{{ one_news.preview }}</p>
        </article>
        {% endfor %}

        {#<div class="prev-next-links"><a href="# ">← <u>предыдущая</u></a> <a href="# "><u>следующая</u> →</a></div>
        <div class="paginator">
            <a href="# ">1</a>
            <span class="current">2</span>
            <a href="# ">3</a>
            <a href="# ">4</a>
            <a href="# ">5</a>
            <span class="placeholder">...</span>
            <a href="# ">11</a>
            <a href="# ">12</a>
        </div>#}
    </div>
{% endblock %}