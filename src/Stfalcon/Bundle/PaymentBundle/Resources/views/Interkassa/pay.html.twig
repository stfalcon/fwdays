{% extends 'StfalconPaymentBundle::layout.html.twig' %}

{% block content %}
    {# todo название и стоимость должны тянуться с события/билета #}
    {# todo разобраться с array('property_path' => false), для коротких имен элементов в форме #}
    <h2>Оплата участия в конференции Zend Framework Day</h2>

    <div class="info-msg-block">
        <p>
            Если вам нужно оплатить участие нескольких человек или вы можете оплатить безналом, тогда пишите нам на <a href="mailto:orgs@fwdays.com">orgs@fwdays.com</a>. В письме укажите "Фамилия Имя e-mail" каждого участника и на кого выписывать счет.
        </p>
    </div>

    <form action="https://interkassa.com/lib/payment.php" method="post" enctype="application/x-www-form-urlencoded" accept-charset="cp1251">
        {{ form_widget(form) }}
                {#{{ form_row(form.amount) }}
        <input type="hidden" name="ik_payment_id" value="{{ form.all['value']['ik_payment_id'] }}">
        <input type="hidden" name="ik_shop_id" value="{{ form.all['value']['ik_shop_id'] }}">
        <input type="hidden" name="ik_payment_amount" value="{{ form.all['value']['ik_payment_amount'] }}">
        <input type="hidden" name="ik_payment_id" value="{{ form.all['value']['ik_payment_id'] }}">
        <input type="hidden" name="ik_payment_desc" value="{{ form.all['value']['ik_payment_desc'] }}">
        <input type="hidden" name="ik_paysystem_alias" value="">
        <input type="hidden" name="ik_baggage_fields" value="">
        <input type="hidden" name="ik_sign_hash" value="{{ form.all['value']['ik_sign_hash'] }}">
        <input type="submit" value="Оплатить участие" />#}
    </form>

{% endblock %}